# Copy to .env for local development. Never commit real credentials.
DATABASE_URL=postgres://slotsure:slotsure_dev_password@localhost:5432/slotsure
PORT=3001
HOST=127.0.0.1
LOG_LEVEL=info
HOLD_DURATION_SECONDS=300
# Production identity provider is TBD — Hospital Decision Required.
AUTH_MODE=development
# Set to true only when demonstrating the UI without the API. This must remain false for real booking tests.
SLOTSURE_USE_MOCKS=false
